"""
의료급여 본인부담률 시스템 프롬프트 모듈 (week-2 참조)
"""

from typing import Dict, Optional

# ---------------------------------------------------------------------------
# 공통 지시 (출력 형식) - Pydantic CopaymentResponse 스키마에 맞춤
# ---------------------------------------------------------------------------

# 반드시 제공된 Context의 내용만을 바탕으로 답변하세요.
# 질문에 '응급상황', '1종/2종', '선택의료급여기관' 등의 구체적인 조건이 있다면, 해당 조건에 맞는 **예외 조항이나 단서 조항을 일반 규칙보다 우선하여 적용**하세요.
# 금액이나 비율을 답할 때는 가장 최신의 구체적인 수치를 사용하세요.
_OUTPUT_RULES = """


반드시 아래 JSON 형식으로만 응답하세요. JSON 바깥에 설명 문장을 붙이지 마세요.
{
  "question": "입력받은 최종 질문 내용",
  "expected_answer": "최종 답만 간결하게 (예: 5%, 무료, 210,000원)",
  "source_section": "정답 근거가 있는 PDF 섹션 또는 표의 이름",
  "evidence_text": "정답을 도출한 문서 내 한두 문장의 원문 리터럴",
  "conditions": ["정답 판단에 쓰인 조건들(예: 1종, 65세이상, 틀니)"]
}
"""
# - **추론 우선순위 규칙**: 질문에 '응급상황', '1종/2종', '특정 상병' 등 구체적인 조건이 포함된 경우, 제공된 Context에서 해당 조건에 반응하는 **'예외 조항', '단서 조항(다만, 단, 예외로 등)'을 일반적인 이용 절차나 기본 수치보다 최우선하여 답변의 근거**로 삼으세요.
_COT_INSTRUCTION = f"""
당신은 의료급여 본인부담률 질문에 대해 Golden Dataset 형식의 구조화된 답변을 생성하는 보조 도구입니다.
최우선 목표는 `expected_answer`를 정확한 형식으로 맞히는 것입니다.
`source_section`과 `evidence_text`는 반드시 함께 제공된 참조 데이터(검색된 내용)에서만 가져와야 합니다.

내부 판단 단계:
1) 질문에서 핵심 조건(conditions)을 추출합니다.
2) 참조 데이터에서 해당 조건에 맞는 섹션(source_section)을 찾습니다.
3) 해당 데이터 내에서 정답 수치(expected_answer)를 확인합니다.
4) 근거가 된 문장(evidence_text)을 그대로 발췌합니다.


- `evidence_text`는 추론 결과가 아니라 참조 데이터에 적힌 실제 문구여야 합니다.

{_OUTPUT_RULES}
"""

def build_rag_system_prompt(context: str) -> str:
    """
    RAG에서 검색된 문맥(context)을 포함한 시스템 프롬프트를 생성합니다.
    """
    return f"""## 참조 데이터 (검색된 정보)
{context}

---
{_COT_INSTRUCTION.strip()}
"""
