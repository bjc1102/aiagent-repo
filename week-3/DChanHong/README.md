# 3주차 과제 성찬홍 (Week 3)


## 1. 이론 과제: RAG의 이해

### 1-1. RAG란 무엇이고, 왜 필요할까?

**RAG(Retrieval-Augmented Generation)란?**

- RAG는 LLM이 이미 머릿속에 외우고 있는 지식(내부 가중치)에만 의존하지 않고, 질문을 받는 순간 **외부의 거대한 서고에서 관련된 책을 찾아(Retrieval) 그 내용을 참고하여 답변을 생성(Generation)하는 기술**입니다.
- 검색으로 찾은 외부 지식을 LLM에 주입(augmented)하여 응답을 생성(generation)하는 방법입니다.
- 해당 방법은 LLM의 내부 가중치(파라메트릭 메모리)에만 의존하지 않고 외부 문서를 결합하여 답변을 생성하는 구조를 가집니다.
    - **파라메트릭 메모리(Parametric Memory):** 모델이 학습을 통해 뇌 세포(가중치)에 새겨넣은 장기 기억입니다.
    - **논-파라메트릭 메모리(Non-parametric Memory):** 모델 외부(Vector DB 등)에 저장된 백과사전이나 최신 문서들입니다. RAG는 이 두 가지를 결합합니다.

**RAG가 필요한 이유는?**

- **토큰 제한을 극복할 수 있습니다. (좁은 책상 vs 무한한 창고)**
    - 문서가 수백, 수천 건으로 늘어나면 LLM이 처리할 수 있는 컨텍스트가 초과하게 되는데 RAG는 필요한 정보만 찾아서 제공하기에 이를 극복할 수 있습니다.
    - 비용 및 운영 효율성을 높여줍니다.
        - 모델을 다시 학습시키는 것(fine-tuning)은 막대한 컴퓨팅 자원이 필요하지만, RAG는 문서 저장소(vector DB)만 업데이트하면 즉시 반영해줍니다.

⇒ RAG는 수천 권의 책 중 질문에 딱 맞는 **'포스트잇 몇 장'**만 골라냅니다. 좁은 책상 위에는 그 포스트잇만 올려주면 되기 때문에, 전체 문서가 아무리 방대해도 모델은 무리 없이 정확한 답을 내놓을 수 있습니다.

- **비용 및 운영 효율성을 높일 수 있습니다.**
    - 새로운 지식이 생길 때마다 모델을 다시 학습시키는 것(Fine-tuning)은 학생에게 **교과서 전체를 다시 외우게 하고 시험을 다시 치게 하는 것**과 같습니다. 시간과 돈(컴퓨팅 자원)이 엄청나게 듭니다.

⇒ RAG는 학생 옆에 최신 백과사전을 놓아주는 **'오픈북 테스트'** 방식입니다. 지식이 바뀌면 사전의 페이지만 갈아 끼우면 됩니다. 모델을 새로 가르칠 필요가 없어 훨씬 경제적이고 빠릅니다.

- **할루네이션(환각)을 방지할 수 있습니다.**
    - LLM은 학습 데이터가 없다면 내용을 그럴듯하게 지어내는 경향이 있는데, 외부의 신뢰할 수 있는 데이터베이스를 참조함으로써 응답의 정확성과 신뢰성을 높일 수 있습니다.
        - **근거 기반 답변:** RAG는 답변을 생성하기 전 신뢰할 수 있는 외부 문서를 먼저 읽습니다. "이 문서에 근거해서 답해줘"라고 명령하는 것과 같기 때문에, 근거 없는 거짓말을 할 확률이 현격히 줄어듭니다.
        - **출처 제시:** 답변과 함께 참고한 문서의 링크나 출처를 제공할 수 있어 사용자가 정보의 신뢰도를 직접 검증할 수 있습니다.

⇒ RAG는 답변하기 전에 반드시 **'참조 서류'**를 먼저 읽습니다. "이 서류의 3페이지에 따르면..."이라고 답하게 하므로 지어낼 확률이 줄어듭니다.

- **정보의 최신성을 유지할 수 있습니다.**
    - LLM의 학습 시점 이후에 발생하는 정보를 알 수 없는 한계가 존재하는데, 문서 저장소 갱신만으로 이를 쉽게 해결할 수 있습니다.
        - **학습 기반 모델:** 어제 일어난 뉴스나 오늘 아침의 주가 정보를 물으면 엉뚱한 대답을 하거나 모른다고 합니다.
        - **RAG 기반 모델:** 실시간 뉴스나 최신 데이터베이스에서 정보를 먼저 찾은 뒤 답변하므로, 모델을 새로 학습시키지 않고도 **최신 정보**를 제공할 수 있습니다.

---

### 1-2. System Prompt와 RAG 방식의 차이는 무엇인가?

System Prompt와 RAG는 모두 LLM의 답변을 제어하는 기술이지만, **'데이터를 어디에 담아두느냐'**와 **'얼마나 많은 정보를 처리하느냐'**에서 결정적인 차이가 있습니다.

**(1) System Prompt**
→ 답변의 ‘형식’과 ‘태도’를 결정하는 데 특화되어 있습니다.
- 특징 : 모든 대화의 시작점에 위치하며, 대화 내내 모델이 유지해야 할 규칙입니다.
- 장점 : 구현이 쉽고 추가 비용이 많이 들지 않습니다.
- 한계 : 방대한 양의 데이터를 넣으면 토큰 비용이 급증하고, 모델이 뒤쪽의 명령어를 잊어버리는 ‘중간 유실’ 현상이 발생할 수 있습니다.

**(2) RAG**
→ 모델이 학습하지 않은 ‘새로운 지식’이나 ‘방대한 문서’를 참조하며, 답변의 내용과 근거를 보강하는데 특화되어 있습니다.
- 특징 : 사용자의 질문이 들어오는 순간, 수만 개의 문서 중 관련된 것만 골라내어 프롬프트에 동적으로 넣어줍니다.
- 장점 : 실시간 정보 업데이트가 가능하며, 수천 페이지의 메뉴얼도 처리할 수 있습니다.
- 한계 : 검색 시스템(Vector DB, Embedding) 구축을 위한 인프라 비용과 기술적 복잡도가 따릅니다.

| 구분 | System Prompt (시스템 프롬프트) | RAG (검색 증강 생성) |
| --- | --- | --- |
| **데이터 위치** | 프롬프트 상단 (컨텍스트 윈도우 내부) | 외부 데이터베이스 (Vector DB 등) |
| **용량 제한** | 모델의 토큰 제한에 걸림 (매우 작음) | 사실상 무제한 (수백만 개의 문서 가능) |
| **데이터 제공 방식** | 사람이 직접 system prompt에 수동으로 삽입 | 시스템이 질문에 맞는 문서를 자동으로 검색하여 제공 |
| **비용 효율성** | 매 요청마다 전체 문서가 들어가 막대한 토큰 비용 발생 | 검색된 관련 청크(문서 조각)만 포함하여 토큰 비용 크게 절약 |
| **최신성 및 유지보수** | 새로운 지식이나 데이터 변경 시 프롬프트를 일일이 수동으로 수정 | 문서 저장소(Vector DB)에 새 문서를 추가해 갱신하면 자동으로 최신 지식 반영 |

---

### 1-3. RAG 파이프라인의 전체 흐름

**1. Indexing (색인 과정 → 도서관 구축 단계)**
→ 외부 데이터를 검색 가능한 형태로 준비하는 단계입니다. 모델이 나중에 정보를 잘 찾을 수 있도록 데이터를 미리 정리해서 창고에 넣는 과정입니다. 단순한 단어 매칭(키워드 검색)이 아닌, 텍스트의 맥락을 이해하는 **'의미론적 검색(Semantic Search)'**을 가능하게 만드는 사전 준비 과정입니다.

- **문서 로딩 (Document)**
    - 외부 지식으로 활용할 원본 데이터(PDF, 텍스트, 웹 문서 등)를 시스템으로 불러옵니다.  LangChain 프레임워크에서는 Document Loader(예: PyPDFLoader)가 이 역할을 수행합니다. 도서관에 새로운 수만 권의 책을 들이는 과정이라고 생각하면 됩니다.
- **청킹 (Chunking)**
    - 로딩된 긴 문서를 모델이 처리하기 좋은 적절한 크기의 조각(Chunk)으로 분할합니다. 보통 200~1000 토큰 크기로 나누며, 문맥이 끊어지는 것을 방지하기 위해 청크 간에 일부분이 겹치도록(overlap) 설정합니다. 두꺼운 책을 한 장 한 장 낱개 페이지로 분리하는 과정입니다.
- **임베딩 (Embedding)**
    - 분할된 텍스트 청크를 컴퓨터가 의미를 계산할 수 있도록 부동 소수점 숫자의 배열(벡터)로 변환합니다. 텍스트의 의미가 비슷할수록 벡터 공간 내에서 서로 가까운 거리를 가지게 됩니다. 각 페이지의 내용을 분석해 '정치', '경제', '요리' 같은 좌표 값을 매기는 작업입니다.
- **벡터 저장 (Vector Store)**
    - 임베딩된 벡터 데이터를 전용 데이터베이스에 저장합니다. FAISS, Chroma, Pinecone 등과 같은 벡터 저장소는 수많은 벡터 중에서 질문과 가장 유사한 벡터를 매우 빠르게 찾아내는 근사 최근접 이웃(ANN) 검색 연산에 최적화되어 있습니다. 좌표 값이 매겨진 페이지들을 찾기 쉽게 전용 서고에 꽂아두는 것입니다.

**2. Retrieval & Generation (검색 및 생성: 실시간 답변 단계)**
→ 사용자의 질문에 대해 실제 응답을 반환하는 실시간 처리 과정입니다. 사용자가 질문했을 때 창고에서 정보를 꺼내 답변을 만드는 과정입니다.

- **질문 벡터화 (Query Embedding)**
    - 사용자가 질문을 입력하면, 시스템은 문서를 임베딩할 때 사용했던 것과 동일한 임베딩 모델을 사용하여 사용자의 질문을 벡터로 변환합니다. 사용자가 "어제 축구 결과 알려줘"라고 하면, 이 질문의 핵심 의미 좌표를 계산합니다.
- **검색 (Retrieval)**
    - 질문 벡터와 벡터 저장소에 저장된 문서 벡터들 간의 **코사인 유사도(Cosine similarity)**를 계산하여, 의미적으로 가장 관련성이 높은 상위 K개(Top-K)의 문서 청크를 추출합니다. 비서가 창고로 달려가 질문 좌표와 가장 비슷한 위치에 꽂힌 페이지 3~5장을 뽑아옵니다.
- **생성 (Generation)**
    - 검색을 통해 얻은 외부 문서 조각(Context)들을 원래의 사용자 질문과 결합하여 LLM에 전달할 새로운 프롬프트를 구성합니다. LLM은 이렇게 제공된 신뢰할 수 있는 외부 지식을 바탕으로 답변을 생성하므로, 사실이 아닌 내용을 지어내는 환각(Hallucination) 현상을 방지하고 정확한 답변을 제공하게 됩니다. 
    비서가 질문과 함께 뽑아온 페이지들을 모델의 책상 위에 올려두며 말합니다. **"이 페이지들을 참고해서 답변해줘."** 그리고 학생이 비서가 가져다준 페이지를 보고 정확한 근거를 바탕으로 답안지를 작성합니다.

---

## 2. RAG에서 사용되는 용어

**Chunking**
- 긴 원본 문서를 LLM이 처리하기 좋은 크기의 작은 텍스트 조각으로 분할하는 과정을 말합니다.
- `chunk_size`: 분할되는 하나의 텍스트 조각 크기(예: 토큰 수)를 결정합니다.
- `chunk_overlap`: 조각이 나뉠 때 문맥이 손실되는 것을 방지하기 위해 앞뒤 청크 간 일부분이 겹치도록 설정하는 크기를 의미합니다.

**Embedding**
- 텍스트의 의미를 컴퓨터가 이해하고 거리를 계산할 수 있도록 부동 소수점 숫자의 배열(벡터)로 변환하는 기술입니다.

| **모델 계열** | **모델명** | **특징 및 장점** | **기본 차원 (Dimensions)** | **비고** |
| --- | --- | --- | --- | --- |
| **OpenAI** | **text-embedding-3-small** | 가성비와 성능의 균형이 가장 뛰어난 모델입니다. 이전 세대(ada-002)보다 성능은 높고 비용은 저렴합니다. | **1,536** | 차원 축소 가능 |
| **OpenAI** | **text-embedding-3-large** | 가장 높은 성능을 제공하는 최신 모델로, 복잡한 다국어 작업 및 전문 지식 검색에 유리합니다. | **3,072** | 차원 축소 가능 |
| **Google** | **Gecko (text-embedding-004)** | 구글의 최신 모델로, 매우 가벼우면서도 검색 성능이 뛰어납니다. 한국어 포함 다국어 지원이 강력합니다. | **768** | Vertex AI 기반 |
| **Open Source** | **BGE / E5 (HuggingFace)** | HuggingFace에서 제공되는 모델들로, 특정 도메인에 맞춰 직접 튜닝(Fine-tuning)이 가능하며 보안이 중요한 폐쇄망 환경에 적합합니다. | **384 ~ 1,024** | 로컬 서버 운영 가능 |

**Vector Store**
- 임베딩된 고정 차원 벡터 데이터들을 저장하고 유사도 기반 검색을 고속으로 수행하는 전용 데이터베이스입니다.

| **저장소명** | **유형** | **주요 특징** | **적합한 사용 환경** |
| --- | --- | --- | --- |
| **FAISS** (Meta) | 라이브러리 | 메타(Meta)에서 개발. **L2 거리 연산** 등 무차별 대입 방식과 근사 근점 이웃(ANN) 검색이 매우 빠름. 별도 서버 없이 로컬에서 동작. | 로컬 개발, 소규모 프로젝트, 초고속 벡터 연산이 필요한 경우 |
| **Chroma** | 오픈소스 DB | 설치가 매우 간편하고 사용법이 직관적임. 내장 임베딩 모델을 지원하며, 로컬 및 서버 모드 모두 지원하는 오픈소스의 강자. | 프로토타입 개발, 오픈소스 생태계 선호, 가벼운 DB가 필요한 경우 |
| **Pinecone** | Managed 서비스 (SaaS) | 완전 관리형 클라우드 서비스. 인프라 설정 없이 API 호출만으로 사용 가능하며, **대규모 데이터 확장성**이 매우 뛰어남. | 대규모 상용 서비스, 인프라 관리 부담을 줄이고 싶은 기업 환경 |
| **Qdrant** | 오픈소스 DB | Rust 언어로 개발되어 성능이 뛰어나며, 벡터 검색 외에 일반 필터링(Payload) 기능이 강력함. 클라우드와 온프레미스 모두 지원. | 정교한 필터링 조건이 필요한 복잡한 데이터 구조의 RAG 서비스 |

**Retriever**
- 사용자의 질문 벡터를 이용해 벡터 저장소에서 가장 가까운 이웃(k-nearest-neighbor) 청크를 찾아내는 컴포넌트입니다.
- `Top-K`는 검색 결과 중 가장 유사도가 높은 상위 K개의 조각을 몇 개까지 추출하여 모델에게 넘겨줄지를 의미합니다.

**Generation**
- 검색을 통해 얻은 청크(Context)와 원본 질문을 결합하여 LLM이 최종 답변을 도출하는 단계입니다.
- 앞선 Retriever가 찾아준 신뢰할 수 있는 외부 문서 조각들을 프롬프트에 포함하여 LLM에 전달합니다. 이를 통해 LLM이 학습 데이터에 없는 내용을 묻더라도 환각(Hallucination) 없이 정확하고 사실에 기반한 답변을 생성하게 됩니다.

---

## 3. 외부 데이터 소스 기반 응답 생성

**LLM 단독 응답 vs 외부 데이터를 검색하여 응답하는 방식의 차이**
→ 가장 큰 차이는 의존하는 지식의 출처와 정보의 투명성 및 최신성에 있습니다.

- **LLM 단독 응답**
    - 모델이 사전 학습 단계에서 파라미터(가중치) 내부에 저장한 '파라메트릭 메모리(Parametric memory)'에만 의존합니다. 학습 시점 이후의 정보를 알 수 없고, 추론 과정이 투명하지 않아 출처를 추적할 수 없습니다. 
    기억이 불확실할 경우 '그럴듯한 거짓말(환각)'을 하는 경향이 있습니다.
- **외부 데이터 검색 응답 (RAG)**
    - 모델 외부의 신뢰할 수 있는 데이터 소스(Vector DB 등)에서 최신 정보를 실시간으로 찾아내어 답변의 근거로 활용합니다. 모델 재학습 없이 문서 저장소 갱신만으로 최신 지식 업데이트가 가능합니다.

**API 호출 흐름: 질문 → 임베딩 → 벡터 검색 → 컨텍스트 구성 → LLM 호출**
1. **질문 (Input):** 사용자가 자연어로 질문을 입력합니다.
2. **임베딩 (Embedding):** 입력된 질문을 숫자 벡터로 변환합니다.
3. **벡터 검색 (Vector Search):** 질문 벡터와 의미적으로 가장 가까운 상위 K개(Top-K)의 문서 청크를 찾습니다.
4. **컨텍스트 구성 (Context Augmentation):** 검색된 조각들을 원래 질문과 결합하여 프롬프트를 생성합니다.
5. **LLM 호출 (Generation):** 구성된 프롬프트를 LLM에 전달하여 정확한 답변을 생성합니다.

**할루네이션 방지에서 RAG가 하는 역할**
- RAG는 LLM이 불확실한 내부 기억력에만 의존하지 않고, 검색을 통해 찾아낸 **신뢰할 수 있는 외부 지식(팩트)**를 직접 주입하는 안전장치 역할을 합니다. 이를 통해 정보 부족으로 발생하는 '추측성 답변'을 원천 차단합니다.

---

## 4. LangChain 기반 RAG 파이프라인 구조

### RAG 구축 프레임워크

- **LangChain (랭체인)**
    - **특징:** LLM 애플리케이션 개발의 표준 프레임워크로, 에이전트, 메모리 관리 등 범용적인 기능을 체인 형태로 연결합니다.
    - **장점:** 생태계가 크고 커뮤니티 지원이 활발하여 확장성이 매우 좋습니다.

| **단계** | **LangChain 컴포넌트** | **상세 역할 및 특징** |
| --- | --- | --- |
| **문서 로딩** | **Document Loader** | 다양한 형식(PDF, HTML 등)의 데이터를 가져와 `Document` 객체로 변환합니다. (예: `PyPDFLoader`) |
| **청킹** | **Text Splitter** | `RecursiveCharacterTextSplitter`는 문맥을 보존하며 적절한 토큰 제한에 맞춰 분할하는 권장 도구입니다. |
| **임베딩** | **Embeddings** | 텍스트 의미를 고차원 숫자 벡터로 변환합니다. (예: `OpenAIEmbeddings`) |
| **벡터 저장** | **Vector Store** | 임베딩된 벡터와 원본 텍스트를 저장하고 고속 유사도 검색을 수행합니다. (예: `Chroma`, `FAISS`) |
| **검색 + 생성** | **Retriever + Chain** | **Retriever**: 저장소에서 관련 청크를 추출합니다. **Chain**: 검색 데이터와 질문을 결합해 최종 답변을 생성합니다. |


## 2. 실습 과제: 의료급여 정책 RAG 최적화

### 2-1. 사용한 프레임워크 및 실행 환경
- **Framework**: LangChain (`langchain-openai`, `langchain-google-genai`)
- **LLM**: Gemini 3 Flash Preview (`gemini-3-flash-preview`)
- **Embedding Model**: OpenAI `text-embedding-3-small`
- **Vector Store**: Chroma (Local Persistence)
- **Environment**: MacOS / Python 3.12+ / `.venv`

### 2-2. Golden Dataset 구축 과정 — 질문 설계 시 고려한 점

#### 1) 의료급여 PDF를 청킹할 때 가장 큰 어려움이 무엇일지 예측하세요
- 복잡한 표구조 청킹이 가장 문제가 될 것으로 예상됩니다.
    - 표 설명과 수치가 섞여 있어서, 단순 글자 수 기준으로 `chunk_size`로 청킹할 경우 불완전한 정보가 담길 수 있습니다.
    - 표 형태의 경우, 표의 행과 열이 중간에 끊겨 조건이 유실될 수도 있을 것으로 예상됩니다.

#### 2) Golden Dataset의 5문제 중 어떤 난이도의 질문에서 검색 실패가 많을지 예측하고 이유를 설명하세요
- Hard 단계인 5번 질문이 가장 어려울 것으로 예측됩니다.
    - 이유는, 본문 표(p.9)의 일반원칙과 FAQ(p.25)의 예외 조항을 결합해야 하는 유기적인 복합 추론과정이 필요하기 때문입니다.

#### 3) Golden Dataset 명세 (5문항)

| **ID** | **질문 (Question)** | **정답 (Expected Answer)** | **근거 FAQ 번호 및 페이지** | **난이도** | **조건 조합 (Conditions)** | **설계 의도 및 검색 복잡도** |
| --- | --- | --- | --- | --- | --- | --- |
| **q01** | 65세 이상 1종 수급권자가 틀니를 할 경우 본인부담률은 몇 %인가요? | **5%** | 본문 4번 표 (p.10) | Easy | `1종` + `65세 이상` + `틀니` | **단일 표 행/열 매칭:** 특정 연령과 종별에 따른 수치를 평면적인 표에서 그대로 읽어오는 수준입니다. |
| **q02** | 희귀질환자가 희귀질환 외 다른 병으로 3차 병원(상급종합병원)에 갈 때 의뢰서가 필요한가요? | **아니요, 모든 질환에 대해 의뢰서 없이 진료가 가능합니다.** | **FAQ Q3** (p.17) | Easy | `희귀질환자` + `의료급여의뢰서` + `예외` | **키워드 직접 검색:** FAQ에 명시된 "희귀질환자 의뢰서 없이 진료 가능"이라는 문장을 직접적으로 찾는 단계입니다. |
| **q03** | 2종 수급권자인 10세 아동이 입원했을 때, 진료비와 식대의 부담률은 각각 얼마인가요? | **진료비 3%, 식대 20%** | **FAQ Q51** (p.34) | Medium | `2종` + `10세(아동)` + `입원` + `식대` | **다중 항목 추출:** '아동'이라는 연령 조건과 '입원' 상황에서 '진료비'와 '식대'라는 두 가지 서로 다른 결과값을 동시에 찾아야 합니다. |
| **q04** | 15세 이하 아동이 제1차의료급여기관(의원)의 의뢰서 없이 바로 제2차 병원에 가도 급여가 적용되나요? | **네, 15세 이하 아동은 단계별 진료 예외 사항에 해당하여 가능합니다.** | **FAQ Q14** (p.317) | Medium | `1종` + `복합추나` + `디스크/협착증` | **상세 카테고리 필터링:** '추나요법'이라는 큰 분류 안에서 '복합추나'를 찾고, 그 안에서도 '특정 상병(디스크)'이라는 세부 조건을 한 번 더 필터링해야 합니다. |
| **q05** | 선택의료급여기관 이용자가 응급상황으로 타 병원 응급실에서 외래 진료를 받은 경우 본인부담금은 얼마인가요? | **본인부담금 면제(무료)입니다.** | **FAQ Q25** (p.25) | Hard | `선택의료급여기관` + `응급상황` + `타 기관 이용` + `본인부담금` | **원칙과 예외의 결합:** "선택기관 외 이용 시 전액 본인부담"이라는 원칙과 "응급상황 시 면제"라는 예외 규정을 논리적으로 연결해야 하므로 검색 및 추론 난이도가 가장 높습니다. |

### 2-3. RAG Indexing 파이프라인 구성

#### 1) 문서 로더 (Document Loader)
- **도구**: `pdfplumber` (커스텀 구현)
- **설정**: 단순 텍스트 추출이 아닌 `extract_tables()`를 사용하여 문서 내의 복잡한 본인부담률 표를 감지하고, 이를 **마크다운(Markdown) 형식으로 변환**하여 저장함. 이를 통해 표의 행/열 관계(종별, 연령별 비율 등)를 모델이 정확히 이해하도록 구성.

#### 2) 청킹 전략 (Chunking)
- **도구**: `RecursiveCharacterTextSplitter`
- **설정**: 
    - `chunk_size`: **500** (데이터의 밀도를 높이고 특정 규정에 집중하기 위해 기존 1000에서 하향 조정)
    - `chunk_overlap`: **100** (청크 간 문맥 단절 방지)
- **특이사항**: 표 데이터의 경우 청크 경계에서 잘리지 않도록 표 전체를 하나의 독립된 청크로 처리하는 로직 적용.

#### 3) 임베딩 및 벡터 저장소
- **Embedding**: OpenAI `text-embedding-3-small` (1536 차원)
- **Vector Store**: `Chroma` (Local Persistence)
- **검색 방식**: 유사도 기반 벡터 검색 (Similarity Search)


### 2-4. 검색 품질 확인 결과 (실험 기록 및 분석)

#### 1) 1차 기록: 기본 환경 (성공률 60%)
- **환경**: `PyPDFLoader`, `k=5`, `chunk=1000/200`

| **질문 ID** | **난이도** | **검색 결과** | **검색된 청크 요약** |
| --- | --- | --- | --- |
| **q01** | Easy | **성공** | 특정 연령/종별 틀니 본인부담률 표 매칭 성공. |
| **q02** | Easy | **성공** | 희귀질환자 의뢰서 면제 조항 직접 검색 성공. |
| **q03** | Medium | **성공** | 아동 입원 진료비/식대 규정 추출 성공. |
| **q04** | Medium | **실패** | 복합추나(30%) 규정을 놓치고 일반 추나(20%)로 오답. |
| **q05** | Hard | **실패** | 응급상황 면제 규정을 찾지 못함. |

**[1차 실패 원인 분석]**
- **q04**: `chunk_size`가 커서 표 내부의 세부 항목이 뭉개지거나 검색 순위에서 밀림.
- **q05**: 일반 유료 규칙의 키워드 강도가 강해 예외 조항이 Top-K(5) 내에 포함되지 못함.

---

#### 2) 2차 기록: 데이터 구조 개선 (성공률 80%)
- **환경**: **`pdfplumber + 표 마크다운 변환`**, `k=10`, `chunk=500/100`

| **질문 ID** | **난이도** | **1차 결과** | **2차 결과 (개선)** | **비고** |
| --- | --- | --- | --- | --- |
| **q04** | Medium | 실패 (20%) | **성공 (30%)** | **표의 행/열 관계 보존으로 정확한 수치 추출** |
| **q05** | Hard | 실패 | **실패** | 정보 혼선(유료 vs 면제) 및 추론 가이드 부족 |

**[2차 실패 원인 분석]**
- **q05**: 검색 결과 내에 '면제'와 '유료' 정보가 공존했으나, 모델이 더 길게 설명된 '일반 유료 규칙'에 닻을 내리는 **Anchor Bias** 발생.

---

#### 3) 3차 기록: 프롬프트 논리 최적화 (성공률 100%)
- **환경**: **`k=15` 확장**, **예외 조항 우선순위 부여 (Logical Priority) 프롬프트 적용**

| **질문 ID** | **난이도** | **1차 결과** | **2차 결과** | **최종 결과** |
| --- | --- | --- | --- | --- |
| **q04** | Medium | 실패 | 성공 | **성공** |
| **q05** | Hard | 실패 | 실패 | **성공 (면제)** |

**[최종 개선 성과]**
- **논리적 우선순위**: "질문에 특정 예외 조건(응급 등)이 있다면 일반 규칙보다 단서 조항을 우선하라"는 지침을 통해 모델의 판단력을 획기적으로 개선하여 **100% 정답률 달성**.

---

### 2-5. 인사이트 및 다음 단계 (Retrieval + Generation)

#### 1) 구조화된 답변 추출 (Pydantic Schema)
- **도구**: LangChain `with_structured_output`
- **목적**: LLM이 자유로운 문장으로 답변할 경우 발생하는 파싱 오류를 방지하고, Golden Dataset의 정답 형식(수치, 근거 문구)을 100% 준수하도록 강제함.
- **스키마 정의**:
  - `answer`: 최종 본인부담금/부담률 (예: 5%, 1,000원)
  - `evidence_text`: 답변의 근거가 된 문서 내 원문 발췌
  - `reasoning`: 정답 도출까지의 논리적 추론 과정
#### 2) 시스템 프롬프트 및 COT(Chain-of-Thought) 설계
- **기법**: 단계별 추론 가이드 제공
  - 1단계: 질문에서 핵심 조건(종별, 연령, 질환 등) 추출
  - 2단계: 검색된 Context 내 최적의 섹션 매칭
  - 3단계: **예외 조항 유무 확인 (가장 중요)**
  - 4단계: 최종 수치 도출
- **특수 지침 (Logical Priority)**: "일반 규칙과 예외 조항이 충돌할 경우, 질문의 상황과 매칭되는 예외/단서 조항을 최우선 근거로 삼으라"는 로직을 명시하여 환각(Hallucination) 현상을 차단함

#### 3) 주요 인사이트
- **데이터 파싱의 중요성**: PDF 내 표 데이터는 단순 텍스트 추출 시 구조적 정보가 유실되어 RAG의 치명적인 오답 원인이 됨을 확인했습니다. `pdfplumber`를 통한 **구조화된 마크다운 변환**이 정답률 향상의 가장 큰 동력이 되었습니다.
- **검색 개수(K)와 노이즈의 관계**: 특정 예외 조항은 일반 규칙에 비해 데이터 양이 적으므로, `Top-K`를 충분히 확보(5 → 15)하되 LLM이 노이즈 사이에서 올바른 정보를 고를 수 있는 **추론 로직**이 반드시 병행되어야 함을 배웠습니다.
---

## 3. 가설과 실제 결과 비교 및 최종 결론

본 프로젝트를 시작하기 전 세운 가설과 실제 구현 및 테스트를 통해 도출된 결과를 다음과 같이 비교했습니다.

### 3-1. 가설 검증 결과

| **가설 항목** | **예측 내용 (Hypothesis)** | **실제 결과 (Actual)** | **검증 및 극복 방법** |
| :--- | :--- | :--- | :--- |
| **청킹(Chunking)의 어려움** | 복합적 표 구조의 경우 단순 텍스트 분할 시 정보의 무결성이 유실될 것임. | **가설 적중.** 일반 로더 사용 시 표의 행/열 값이 섞여 q04번 등에서 오답 발생. | **`pdfplumber`**를 도입하여 표를 마크다운 형식으로 분리 추출함으로써 구조를 완벽 보존함. |
| **최고 난이도 문항(q05) 예측** | 일반 규칙과 예외 조항이 혼재된 q05번이 가장 어려울 것으로 예상. | **가설 적중.** 검색(Retrieval) 성공 후에도 생성(Generation) 단계에서 AI가 일반 규칙을 선택하는 Anchor Bias 발생. | **프롬프트 엔지니어링**을 통해 "단서 조항 우선순위"라는 논리 지침(Logical Priority)을 주입하여 해결함. |

### 3-2. 최종 결론
이번 의료급여 RAG 최적화 실습을 통해, 고성능 RAG 시스템 구축은 단순히 모델 성능에 의존하는 것이 아니라 **"정밀한 데이터 전처리(Table-aware Parsing)"**와 **"검색 결과의 우선순위를 제어하는 도메인 특화 프롬프트 설계"**가 결합되어야 함을 확인했습니다. 

단순 검색을 넘어, 법률/지침 문서와 같은 예외 규정이 많은 도메인에서는 **Search-then-Reason(검색 후 추론)** 기반의 단계별 가이드라인이 Golden Dataset 100% 달성에 가장 결정적인 역할을 수행했습니다.

